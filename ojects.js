/*const student = {
  name: "pishpa",
  age:22,
  action:function(callback){
   console.log("performing function on:" this.name());
   callback();
  }
}
student.action(function(){
 console.log("action completed");
});
// object with a callback function
const areaRectangle = {
  name: "pushpa",
  age: 22,
  branch: "bio",
  grades: [85, 90, 80, 75],
  calcavg: function () {
    let sum = 0;
    for (let i = 0; i < this.grades.length; i++) {
      sum += this.greades.length;
    }
    return sum / this.grades.length;
  },
};
console.log("average" + student.calcavg());*/
class Person{
 constructor(name,age){
  this.name = name;
  this._age
 }
}
